// Preenchido por WP4 (Obrigações + Configurações): rotas (lazy) e itens de navegação da feature "das".
import type { AppModule } from '@/app/registry';

export const dasModule: AppModule = { id: 'das', routes: [], nav: [] };
