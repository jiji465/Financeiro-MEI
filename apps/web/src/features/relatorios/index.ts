// Preenchido por WP5 (Dashboard + Relatórios): rotas (lazy) e itens de navegação da feature "relatorios".
import type { AppModule } from '@/app/registry';

export const relatoriosModule: AppModule = { id: 'relatorios', routes: [], nav: [] };
