// Preenchido por WP2 (Lançamentos + Importação): rotas (lazy) e itens de navegação da feature "lancamentos".
import type { AppModule } from '@/app/registry';

export const lancamentosModule: AppModule = { id: 'lancamentos', routes: [], nav: [] };
