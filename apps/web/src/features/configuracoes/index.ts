// Preenchido por WP4 (Obrigações + Configurações): rotas (lazy) e itens de navegação da feature "configuracoes".
import type { AppModule } from '@/app/registry';

export const configuracoesModule: AppModule = { id: 'configuracoes', routes: [], nav: [] };
