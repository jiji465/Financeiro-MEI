// Preenchido por WP5 (Dashboard + Relatórios): rotas (lazy) e itens de navegação da feature "dashboard".
import type { AppModule } from '@/app/registry';

export const dashboardModule: AppModule = { id: 'dashboard', routes: [], nav: [] };
