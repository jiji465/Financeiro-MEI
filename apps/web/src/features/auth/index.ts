// Preenchido por P1-C (Web core): rotas (lazy) e itens de navegação da feature "auth".
import type { AppModule } from '@/app/registry';

export const authModule: AppModule = { id: 'auth', routes: [], nav: [] };
