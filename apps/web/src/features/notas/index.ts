// Preenchido por WP3 (Contas + Notas): rotas (lazy) e itens de navegação da feature "notas".
import type { AppModule } from '@/app/registry';

export const notasModule: AppModule = { id: 'notas', routes: [], nav: [] };
