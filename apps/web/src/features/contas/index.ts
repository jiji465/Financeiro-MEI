// Preenchido por WP3 (Contas + Notas): rotas (lazy) e itens de navegação da feature "contas".
import type { AppModule } from '@/app/registry';

export const contasModule: AppModule = { id: 'contas', routes: [], nav: [] };
