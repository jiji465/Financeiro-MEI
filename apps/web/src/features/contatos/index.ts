// Preenchido por WP1 (Contatos): rotas (lazy) e itens de navegação da feature "contatos".
import type { AppModule } from '@/app/registry';

export const contatosModule: AppModule = { id: 'contatos', routes: [], nav: [] };
