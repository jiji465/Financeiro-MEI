// Preenchido por P1-C (Web core): rotas (lazy) e itens de navegação da feature "referencias".
import type { AppModule } from '@/app/registry';

export const referenciasModule: AppModule = { id: 'referencias', routes: [], nav: [] };
